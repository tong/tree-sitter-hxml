; Directives
(class_path) @classpath

(main) @entry

(target) @target

(library) @library

(define) @define

(macro) @macro

(cmd) @command

(cwd) @directory

; Identifiers
(type_name) @type

(package) @package

; Primitives
(directory) @directory

(file) @file

(flag) @flag

; Other
(section) @section

; Library details
(library
  (name) @library.name)

; Define details
(define
  (define_key) @define.key)

; Resource details
(resource) @resource

(resource_name) @resource.name
