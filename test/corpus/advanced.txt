================================================================================
define with value
================================================================================

-D test=value
--define key=complex:value

---

(source_file
  (section
    (define
      (constant))
    (define
      (constant))))

================================================================================
resource with name
================================================================================

-r assets/image.png@my_image
--resource data.txt@config

---

(source_file
  (section
    (resource
      (file)
      (resource_name))
    (resource
      (file)
      (resource_name))))

================================================================================
server addresses
================================================================================

--server-listen stdio
--server-listen localhost:8080

---

(source_file
  (section
    (server_listen
      (server_address))
    (server_listen
      (server_address))))

================================================================================
custom target
================================================================================

--custom-target neko
--custom-target cs=out

---

(source_file
  (section
    (target
      (custom_target))
    (target
      (custom_target))))

================================================================================
run with arguments
================================================================================

--run my.App arg1 arg2

---

(source_file
  (section
    (target
      (type_path
        (pack)
        (type))
      (argument)
      (argument))))

================================================================================
dce modes
================================================================================

--dce std
--dce full
--dce no

---

(source_file
  (section
    (dce
      (dce_mode))
    (dce
      (dce_mode))
    (dce
      (dce_mode))))
